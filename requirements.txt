# ─────────────────────────────────────────────────────────────────────────────
# Agentic Month-End Assistant – Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────

# ── Core LangGraph / LangChain stack ─────────────────────────────────────────
langgraph>=0.2.50               # Graph-based agent orchestration
langchain>=0.3.0                # LLM tooling & chains
langchain-aws>=0.2.0            # Bedrock LLM integration
langchain-community>=0.3.0      # Community tools (web search, etc.)
langchain-core>=0.3.0           # Core abstractions

# ── AWS SDK ───────────────────────────────────────────────────────────────────
boto3>=1.35.0                   # AWS SDK (AgentCore, Bedrock, DynamoDB)
botocore>=1.35.0

# ── Async HTTP (Teams & Slack webhooks) ───────────────────────────────────────
httpx[asyncio]>=0.27.0
aiohttp>=3.10.0

# ── Data validation ───────────────────────────────────────────────────────────
pydantic>=2.8.0
pydantic-settings>=2.5.0        # Settings from env vars

# ── Sandbox / restricted execution ───────────────────────────────────────────
RestrictedPython>=7.2           # AST-based Python sandbox

# ── Pretty console output ─────────────────────────────────────────────────────
rich>=13.8.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
python-dateutil>=2.9.0
orjson>=3.10.0                  # Fast JSON
tenacity>=9.0.0                 # Retry logic
tabulate>=0.9.0                 # Table formatting

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
